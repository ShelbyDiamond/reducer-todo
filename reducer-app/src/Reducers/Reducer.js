export const initialState = {
  toDo: [
    {
      item: "",
      completed: false,
      id: date.now
    }
  ]
}

export const reducer = (state, action) => {
  switch (action.type) {
      case: "New Chore"
      return state
  }
}
