export const initialState = {
  toDo: [
    {
      item: "Feed the neighbor's dogs",
      completed: false,
      id: 3892987589
    }
  ]
}

export const Reducer = (state, action) => {
  switch (action.type) {
    default:
      return state
  }
}
